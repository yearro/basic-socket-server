{
  "name": "socket-server-basic",
  "version": "1.0.0",
  "description": "Socket server basic",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon server/index.js",
    "start": "node server/index",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy-heroku": "git push heroku main",
    "server-logs": "heroku logs -n 1000 --tail"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "socket.io": "^4.5.1"
  }
}
